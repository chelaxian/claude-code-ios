# Retained as source documentation for the former script adapter.
# iGhostty's privileged daemon receives EPERM when execve'ing a #! script on
# Dopamine, so the package now ships the signed Mach-O adapter built from
# src/ighostty-shell.c instead. It ignores `-il` and always starts Claude.
